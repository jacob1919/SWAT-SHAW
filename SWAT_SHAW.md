# SWAT+SHAW：水热耦合试验分支

本项目将 USDA-ARS SHAW 3.0.3 的冠层—积雪—土壤水热过程接入 SWAT+ 的 HRU 日循环，保留 SWAT+ 的流域汇流、管理、植被生长和水质框架。当前为**研究原型**。加拿大案例用于检查程序和过程差异，尚无随附实测流量可用于精度验证。

三个版本已完成 2020-01-01 至 2023-04-30 的完整运行。评价期为扣除 2020 年预热后的 850 天；123 个 HRU 使用 SHAW，19 个城市 HRU 保留原始过程。结果、冬春过程图、数值检查与适用边界见 [加拿大三版本对照报告](reports/canada/REPORT.md)。报告区分陆地 HRU 面积（15.1818012 km²）与含独立水库的出口汇水面积（16.353104 km²），并说明原案例水库放水规则对出口脉冲的影响。

仓库：https://github.com/jacob1919/SWAT-SHAW 。为与既有冻融版本比较，开发分支 `codex/shaw-water-heat` 从官方提交 `61c940f40b5da768edc62a30641d9a1fbebc2708` 起步。最新官方 main 单独保留在 upstream。

## 过程归属

| 过程 | 启用耦合的 HRU | 未启用的 HRU |
|---|---|---|
| 冠层截留、积雪与融雪 | SHAW | 原始 SWAT+ |
| 土壤热传导、液水/冰相变 | SHAW | 原始 SWAT+ |
| 蒸散、垂直水分运动、产流 | SHAW | 原始 SWAT+ |
| 侧向出流 | SHAW 饱和侧向项，按坡长转换为单位面积通量 | 原始 SWAT+ |
| 植被结构、管理、水质、河网与地下水汇流 | SWAT+ | SWAT+ |

城市不透水面、地表水体、暗管排水和化粪系统 HRU 保留原始过程；`shaw_hru_scope.csv` 给出逐 HRU 的启用情况。SHAW 溶质输运和 CO₂ 不参与耦合编译。`vendor/shaw303/Shaw303.original.for` 是未修改的官方来源文件，仅用于溯源和参照试验，其中原有的溶质代码不代表已移植溶质功能。

## 接口约定

- `SWAT_SHAW=1` 启用耦合；未设置或值为 `0` 时保持原始路径。仅支持 SWAT+ 日步长。
- 每个 HRU 保存独立的土柱和 COMMON/SAVE 状态；调用时依次恢复、计算、保存。内核采用双精度 REAL、32 位整数和无填充 COMMON，35 个状态块共 82,808 字节；SWAT+ 主体精度保持不变。**目前只支持串行调用**，不得直接在 OpenMP 线程中并行调用旧 SHAW 内核。
- 土柱约 5 cm 间距，最多 99 节点，最末节点为边界节点、不计入储水。重叠体积矩阵在 SWAT+ 土层与 SHAW 控制体之间转换，初始化检查体积及水量守恒。
- SWAT+ `st` 是扣除萎蔫点水量后的储水；SHAW 保存实际体积液态含水量和体积冰量。冰按水当量 `0.92 × 冰体积` 计入账本。向 SWAT+ 输出可用液水，土柱内部保留冰和低于萎蔫点的液水。
- 土壤密度、质地、孔隙度、导水率来自初始化后的 SWAT+；Campbell 曲线用田间持水量和萎蔫点推导。原案例的 AWC 已通过加拿大官方同编号土壤的四层数据溯源，确认缺少百分数到体积分数的换算。新三版本对照统一使用 0.15、0.14、0.14、0.08；原输入和旧结果保留，详见 [AWC 审计](reports/canada_awc/REPORT.md)。
- 无侧向各向异性观测时，使用垂直饱和导水率并保留 `latq_co` 系数。传入侧向项的有效系数为 `Ksat × latq_co / 坡长`，使 `有效系数 × sin(坡角) × 控制体厚度` 表示单位面积水通量。该参数化需后续独立验证。
- 参考设置仍采用自由排水与约 1.026 m 深处的年均气温恒温边界；该浅边界缺少观测支持。可选择 SHAW 原生估算温度边界开展对照，此选项没有把剖面延伸至 4 m。初始时要求无积雪、冠层截留为空。
- 日降水均匀分至 24 小时；温度按日均温和日温差重建；短波按纬度和日照形状分配并保持日总量；湿度和风速日内保持不变。云量暂取 0.5。这些是假设，不能替代真实逐小时强迫。
- 一种等效植被的 LAI、株高、地上生物量和根深来自 SWAT+；SHAW 可在其冠层内划分多个垂直节点，气孔和植被水力参数暂用明确的默认值。冠层截留、土壤蒸发及雪升华尚未分别输出，SWAT+ `esoil` 在耦合 HRU 中表示总净蒸散扣除蒸腾后的剩余项，可能包含凝结。
- SHAW 残茬物理函数已保留，但本次没有把 SWAT+ 残茬质量映射为 SHAW 残茬层；缺少层厚与容重资料时暂设 `NR=0`。管理导致的土壤物理参数变化也尚未逐日重新传入。这些限制尤其影响农田、枯落物较厚的森林及管理情景解释。
- 上游地表来水和灌溉按液态水加入积水，参与下一小时计算；其他外部土层水分变化按重叠体积转入，假定进入水具有接收土层温度。完整的外部来水热焓输送尚未验证。
- SHAW 内部保留向上、向下的有符号水通量。现有 SWAT+ 养分程序只支持向下渗漏，桥接仅把土层底部日净水通量的正值传给其 `prk` 字段；负值会使旧的逐层硝态氮计算产生虚假养分。向上溶质输运没有耦合，传出的总水通量也暂未单独剔除水汽贡献，不能把保留旧水质程序描述为完整溶质耦合。

## 数值核验

`tools/extract_shaw.py` 自动提取并命名隔离旧内核，保留来源哈希和函数行号。原始 SHAW 的固定冠层水汽容差改为可配置，默认值仍为 0.01；流域桥接使用土壤求解容差 `1e-4`、冠层相对水汽容差 `1e-3`。后者由完整 HRU 时段的容差敏感性与守恒试验选择，见数值审查记录。小时尝试不收敛时，先恢复完整状态，在同一时段划分下尝试包含冠层交换系数温度导数的迭代矩阵；仍失败再二分时段，最多将小时拆为 64 段。两种矩阵求解相同物理方程、使用相同收敛门槛；最终失败即终止，不静默退回原始 SWAT+。

现有检查包括：

1. 两个不同节点数与冠层的土柱独立与交替执行，状态及通量完全相同；最大小时水量残差约 `0.000982 mm`。另有 288 小时冠层出现、生长、落叶及冻融试验，最大残差约 `0.001318 mm`；两项验收限值均为 `0.002 mm`。
2. 外部液水加入与取出的储量守恒检查。
3. 单独编译保留原始 SAVE 变量和溶质代码的官方 SHAW 求解器，以无溶质、相同适配器输入和编译精度进行参照：720 小时、282,960 个物理数值一致。单精度和双精度构建分别通过该检查；不是宣称两种精度之间逐位相同。这个试验验证内核提取与状态隔离，不是对官方文件读取器或所有地表类型的完整验证。
4. 加拿大案例关闭耦合后的输出回归；实际结果见 `reports/canada/regression.json`。
5. 耦合 HRU 的逐日水量账本：储量变化减降水、外部来水及冠层空气控制体变化带来的水汽交换，加净蒸散、径流、底部渗漏、侧向出流。绝对残差超过 `0.1 mm/HRU/day` 时停止。完整运行的累计误差也须报告。
6. 冠层交换系数的解析温度导数与原始 `CANTK` 有限差分比较，最大缩放差约 `1.32e-7`，限值 `1e-5`。
7. 干湿土层根系分配反例、节点排序、零需求和微小通量检查。桥接另要求每个已接受小时的根系吸水与蒸腾差不超过 `1e-6 mm`；根深限制在储水土体内，边界节点不供水。

流域桥接修正了四处冠层储量项迭代系数、一处叶片方程消元残差，以及干旱条件下可能使吸水超过蒸腾的单次根系筛选。根系供水现迭代更新有效根系集合，满足非负通量与总供水约束；不通过事后扣减账本修饰守恒。非强制细分的时间步中迭代上限扩为 40 次，保留原来的大更新细分保护、反向更新减半及收敛门槛。默认关闭修正的路径保留用于官方源码参照；不能把修正路径描述为与原版逐位一致。降雪诊断使用原始湿球温度分类规则，记录截留前的大气降雪。详细推导、重现证据和冠层空气水量交换的说明见 [数值审查记录](reports/NUMERICAL_AUDIT.md)。

`SWAT_SHAW_HRU` 可用于单个 HRU 的完整时段诊断，其余 HRU 使用原始路径。该模式额外输出逐小时水量、蒸腾和根系吸水；可用 `SWAT_SHAW_CAPTURE_HOUR="2022 196 16"` 按“年份、年积日、小时”捕获指定小时的起始状态。完整流域比较会清除单 HRU 选项。完整时段及旧失败日复查见 [土柱检查记录](reports/canada/canopy_solver_checks.json)。这些检查与全流域对照的完成状态分别记录。

修正路径还在原生最小时间步处保留六处大幅更新保护。原代码到达该步长后不再限制这些更新，可能在尚未判定收敛前计算出无效物性；接口现拒绝该次尝试并执行完整小时回退。温度/能量、冰量和基质势的阈值沿用原有条件，收敛门槛不变。

仓库自带 Ames 历史参考输出与当前固定官方可执行文件也存在差异；未改写这些参考文件。另行从该官方可执行文件生成的 Ames 结果，与关闭耦合的版本在 `.testfiles.txt` 指定的 42 个输出文件中一致，记录见 `reports/canada/ames_pristine.json`。因此原有 Ames golden CTest 仍会报告失败，不能将整个 CTest 结果描述为全绿。

## 本机复现

先使用 Intel oneAPI 环境配置并构建 CMake/Ninja 项目。`tools/build_ifx.cmd` 是本机编译环境入口。内核可通过 `cmake -S src/shaw -B build/shaw_kernel` 独立构建。官方参照源码由 `tools/build_shaw_reference.py` 在 build 目录生成，再使用 `tests/reference/CMakeLists.txt` 构建。

加拿大数据保留在本机，未收入 Git：

1. `tools/prepare_canada.py` 从原始案例及既有修正后的土壤文件生成隔离副本，并写入输入哈希清单。
2. `tools/run_canada.py official`、`existing_ft`、`shaw`、`coupling_off` 分别运行四项实验，记录可执行文件哈希、开始/结束时间及退出码。
3. `tools/check_shaw_reference.py` 核验官方 SHAW 参照数值。
4. `tools/check_canada_column_parity.py` 将完整流域中 HRU 1、6、19、98 的轨迹与单独运行比较；此项需要先完成 `tools/debug_canada_hru.py` 的对应诊断运行。
5. `tools/analyze_canada.py` 检查运行完整性，生成逐日、季节、年度统计，以及水文、第二土层温度和 SHAW 土壤冰储量的科学绘图。
6. `tools/write_canada_report.py` 由通过检查的统计和运行标识生成中文报告。

本机工具脚本中的旧项目路径和编译器路径需要在其他机器上配置。已有冻融项目的未提交修改受到保留；未把这些修改搬入此分支。

## 解释边界

加拿大案例的湿度、风和太阳辐射由 SWAT+ 气象发生器提供，没有随附观测流量、雪水当量或土温序列。当前对照不能说明耦合版精度优于其他版本。既有冻融可执行文件还包含非冻融模块修改，三者差异也不能全部归因于冻融公式。原始 SWAT+ 的 CN、融雪、渗漏和蒸散参数无法直接等价为 SHAW 参数，需要后续分模型率定和独立验证。

后续研究应补充逐小时强迫、观测流量与土壤/积雪资料、深层温度及参数敏感性、网格和时间步收敛、整体能量收支、更多下垫面测试，以及水质过程对新水通量的适用性检查。报告须区分已通过的数值检查与这些仍待完成的科学验证。

## 接口审查后的更新

新三版本完整对照及单因素试验见 [修正后加拿大报告](reports/interface_cases/REPORT.md)，原报告作为审查前基线保留。四个代表 HRU 与完整流域逐值一致，八项 SHAW 测试通过，完整流域最大 HRU 日水量残差为 0.008281023 mm。水量账本不等同于能量闭合或实测精度验证。

坡向可由 shaw_aspect.csv 显式给出；缺测时采用水平面辐射近似，侧向流保留实际坡度。气象参考高度按初始植被群落的最大株高固定，风速从明确的 10 m 参考高度换算；温湿度垂直均一仍是近似。新增 snow_liquid_release_mm 及原生 snomlt 在耦合 HRU 中均表示含可能穿雪降雨的雪层液态释放，不能解释为纯相变融雪量。

重现控制和假设详见 [接口说明](reports/INTERFACE_REVIEW.md)。新状态快照增加 8 字节辐射选项，旧二进制土柱捕获文件与新布局不兼容。原生估算热边界和 6 小时降雨实验显示产流对配置高度敏感；仍需深层边界、能量账本、网格/时间步收敛和独立观测验证。
